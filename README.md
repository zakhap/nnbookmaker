# nnbookmaker

nnbookmaker is a browser-based book-layout tool that converts Pandoc-flavored Markdown into paginated PDF-ready output using a unified remark/rehype pipeline, Paged.js for CSS print layout, CodeMirror 6 for live editing, and a token-driven design system (CSS `@layer` cascade) that supports multiple trim sizes and custom themes.
